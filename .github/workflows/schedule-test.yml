name: Schedule Test

on:
  push:
    branches:
      - main
      
  schedule:
    - cron: "*/5 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test scheduled trigger
        run: |
          echo "Schedule triggered"
          date -u